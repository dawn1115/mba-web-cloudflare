# 本地开发 Cloudflare Workers 绑定变量（不会上传到 Cloudflare，仅本地用）
# 复制本文件为 .dev.vars 并填入真实值

# JWT 签名密钥（生产环境必须替换为随机强密码，至少 32 位）
JWT_SECRET=REPLACE_WITH_STRONG_RANDOM_SECRET

# 本地开发时 SQLite 数据库路径（Workers 生产环境由 D1 绑定替代）
DATABASE_URL=file:./dev.db
